import styled from 'styled-components';

export const Container = styled.div`
    width: 100%;
    height: 100vh;
    background: radial-gradient(rgb(40, 40, 40), #000);

    display: flex;
    justify-content: center;
    padding: 50px;
`